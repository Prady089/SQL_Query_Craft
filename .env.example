# Copy this file to .env and fill in your OpenAI key before running locally
OPENAI_API_KEY=your-openai-key-here
# Optional: which model to use
OPENAI_MODEL=gpt-4o-mini
# API URL used by the UI (when running API in a container use http://api:8000/chat)
API_URL=http://localhost:8000/chat
