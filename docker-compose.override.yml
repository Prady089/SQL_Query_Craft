version: '3.8'

services:
  api:
    # Mount local source for live editing during development
    volumes:
      - ./:/app
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload

  ui:
    volumes:
      - ./:/app
    command: python chatbot_ui.py
